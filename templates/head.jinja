{%- set title = data.get( "title" ) %}
{%- set panel = data.get( "panel" ) %}
<head>
{%- if title %}
    <title>
        {{ title }}
        {%- if panel %} - Admin Panel{% endif %}
    </title>
{%- endif %}
</head>